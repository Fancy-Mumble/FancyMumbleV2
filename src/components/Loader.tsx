import { CircularProgress, Container } from "@mui/material";

function Loader() {
    return (
        <Container>
            <CircularProgress />
        </Container>
    )
}

export default Loader;